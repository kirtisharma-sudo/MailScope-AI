fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2
requests==2.32.3
dnspython==2.6.1
networkx==3.3
supabase==2.7.4

# Testing
pytest==8.3.2
httpx==0.27.2

# NOTE: transformers/torch intentionally NOT in this file — see
# requirements-ml.txt. nlp_detector.py only imports them lazily, inside a
# function, and only if a real checkpoint directory exists at
# NLP_MODEL_PATH (it doesn't yet — see models/deberta-phishing/.gitkeep).
# Installing ~2GB of torch on every deploy for code that never runs in the
# current demo state is not "a runtime dependency" yet; it becomes one the
# day a trained checkpoint is added. At that point: pip install -r
# requirements.txt -r requirements-ml.txt. This was verified empirically —
# all 74 tests pass without torch/transformers installed at all.
